{{template "base" .}}

{{define "content"}}
    <div class="container">
        <div class="row>
            <div class="col>
                <h1>About us!</h1>
                <p>Some specifications and defitions.</p>

                <p>Template data: {{index .StringMap "test"}}</p>
            </div>
        </div>
    </div>
{{end}}